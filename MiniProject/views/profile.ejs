<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-5">
        <a class="bg-red-600 px-3 py-1 rounded-md text-sm" href="/logout">Log Out</a>
        <div class="flex item-start gap-2 mt-3">
            <div class="w-15 h-15  rounded-full overflow-hidden">
                <img class="w-full h-full" src="/images/default.webp"/>
            </div>
            <h3 class="text-3xl mt-5"><span class="font-light">Hello</span> <%= user.name %> ✌️</h3>
        </div>
        <h5 class="mb-5">You can create new post.</h5>
        <form action="/post" method="post">
            <textarea placeholder="what's on your mind?" class="block p-2 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-600 rounded-md" name="content"></textarea>
            <input type="submit" value="Create New Post" class="block text-sm bg-blue-500 mt-3 px-5 py-1 rounded-md"/>
        </form>
        <div class="posts mt-15">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach((post)=>{ %>
                    <div class="post w-1/3 p-3 mb-4 border-1 border-zinc-800 rounded-md bg-zinc-700">
                        <h4 class="text-blue-400 mb-2"><%=user.username%></h4>
                        <p class="text-sm tracking-tight"><%=post.content%></p>
                        <small class="mt-4 inline-block"><%= post.likes.length%></small>
                        <div class="bts flex justify-between">
                            <a class="text-blue-500" href="/like/<%=post.id%>"><%= post.likes.indexOf(user.id) === -1 ? "Like" : "Unlike"%></a>
                            <a class="text-zinc-500" href="/edit/<%=post.id%>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
    
</body>
</html>