<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="stylesheet" href="/stylesheets/style.css"> -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 px-10 py-5 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/">Back to home</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <%users.forEach((user)=>{%>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg border-zinc-500 border-2">
                <div class="w-full h-52 rounded-lg bg-zinc-600 overflow-hidden">
                    <img class="w-full h-full object-cover" src="<%=user.image%>" alt="image"/>
                </div>
                <h3 class="text-xl mt-3 tracking-tighter"><%=user.name%></h3>
                <h5 class="text-zinc-500"><%=user.email%></h5>
                <div class="flex justify-between item-centre mt-5">
                    <a class="text-green-500" href="/edit/<%=user.id%>">Edit User</a>
                    <a class="text-red-500" href="/delete/<%=user.id%>">Delete User</a>
                </div>
            </div>
            <%})%>
        </div>
    </div>
</body>
</html>